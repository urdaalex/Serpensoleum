create_file.py -- Creates the "arff" files that are to be read by Irrelevacy parser

measure_irrelevant -- Actual TFidf scoring

Irreleveancy_Scoring.py -- Creates the saved model as `saved_model.pkl`. Can also be used to run tests.

magic_function.py -- magic_function(doc) takes in a json object, and returns 1 if relevant or 0 if irrelevant
